@using Sportify.Data.ViewModels.Disciplines
@using Sportify.Constants

@model Sportify.Data.ViewModels.Sports.SportViewModel

@{
    ViewData["Title"] = "Details";
}

<br />

<h1>@Model.Name</h1>

<hr />

<div class="row">
    <div class="col-md-4">
        <img class="card-img-top" src="@Model.ImageSportUrl" alt="@Model.Name">
    </div>
    <div class="col-md-8">
        <h4>Description</h4>
        <p>@Model.Description</p>
    </div>
</div>

<br />

<h4>Disciplines</h4>
@if (this.ViewData[GlobalConstants.Disciplines] != null)
{
     foreach (var discipline in this.ViewData[GlobalConstants.Disciplines] as IEnumerable<DisciplineViewModel>)
     {
         <hr/>
         <div class="row">
             <div class="col-md-5">
                 @discipline.Name
             </div>
             <div class="col-md-7">
                 @discipline.Description
             </div>
         </div>
     }
}

<br />

<div class="text-center">
    <a asp-area="Sport" asp-controller="Sports" asp-action="AllSports">Back to sports</a>
</div>
